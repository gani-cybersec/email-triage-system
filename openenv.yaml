version: "1"

services:
  app:
    build: .
    command: python app.py
    ports:
      - "8000:8000"

evaluation:
  inference_script: inference.py

env:
  API_BASE_URL: ${API_BASE_URL}
  API_KEY: ${API_KEY}
